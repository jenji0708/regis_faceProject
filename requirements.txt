Flask==2.1.1
pymongo==4.0.1
face_recognition==1.3.0
PyJWT==2.1.0
Werkzeug==2.1.1

# ติดตั้ง Flask และส่วนเสริม
pip install flask flask-pymongo flask-bcrypt flask-jwt-extended

# ติดตั้งไลบรารีสำหรับ MongoDB
pip install pymongo

# ติดตั้งไลบรารีสำหรับการจัดการรูปภาพ
pip install opencv-python face-recognition pillow numpy

# ติดตั้งไลบรารีสำหรับจัดการอีเมล
pip install secure-smtplib
